<%= render "layouts/errors", :object => @comment %>

<%=form_for([@entry,@comment], html: {class: "form-group"}) do |f| %>
  <%= f.label :body %>
  <br>
  <%= f.text_area(:body, rows: "12", cols: "40") %>
  <br>
  <%= f.submit "Store comment", class: "btn btn-primary"  %>
<% end %>
