<% content_for(:title, @entry.title + " | New Comment") %>

<div class="row">
  <div class="col-sm-3"></div>
  <div class="col-sm-6 well">
    <div class="page-header">
      <h1>Add a comment for <%= @entry.title %></h1>
    </div>
    <%= render 'form' %>
  </div>
  <div class="col-sm-3"></div>
</div>
